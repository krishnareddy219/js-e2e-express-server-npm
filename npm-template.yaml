parameters:
  name: npminstall
  displayName: npm installation
  type: string
  default: install 
  values:
    - install
    - run build 


steps:
  - task: Npm@1
    inputs:
      command: 'install'
  - task: Npm@1
    inputs:
      command: 'custom'
      customCommand: 'run build'
